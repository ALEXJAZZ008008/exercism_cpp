#pragma once

#include <vector>
#include <string>
#include <algorithm>

using namespace std;

namespace secret_handshake
{
    vector<string> commands(int number);    
}  // namespace secret_handshake
